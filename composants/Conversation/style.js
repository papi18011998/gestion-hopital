import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    container:{
        backgroundColor: "#FFFFFF",
        margin: 5,
        padding: 10,
        borderRadius: 10,
        maxWidth:"80%"
    }
});